# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
build/
dist/
develop-eggs/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.pytest_cache/
.pytest_cache_win/
test_output*.txt
verification_output.txt
entropy_metrics.json

# Virtual environments
.env
.venv
env/
venv/

# IDE files
.idea/

# VS Code - ignore personal settings but share project configs
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/keybindings.json

# Runtime artifacts
cooling_ledger.jsonl

# VAULT999 runtime files (keep structure, ignore runtime data)
VAULT999/cooling_ledger.json
VAULT999/constitutional_vault.json
VAULT999/*.jsonl

# Cryptographic keys (NEVER commit)
*_private_key.b64
*_private_key.pem

# SSL/TLS certificates (VAULT999 MCP Server)
*.pem
arifos_core/mcp/certs/*.pem

# Misc
*.so
.DS_Store
Thumbs.db

# --- PERSONAL GUIDES (Local Only) ---
guides/
!guides/.gitkeep

# --- APEX EXCLUSIONS v35Ω ---
.vs/
.claude/
!.claude/TEARFRAME.md
!.claude/SECURITY.md
!.claude/CONSTITUTION.md
!.claude/claudebrain/
nul
arifos-test/
.codex_remote/
*.swp

# Gemini CLI
.gemini/

# Gemini CLI config - keep baseline, ignore backups
!config/gemini/*.json
!config/gemini/*.md
config/gemini/backups/

# Temporary staging folders
staging.delete*/
docs/staging/
scripts/bogel_results_*.json

# Ephemeral test output (manual Ollama runs)
tests/arifOS llama test*.md

*.mp4

# v45Ω Housekeeping (2025-12-25)
# Runtime logs and evaluation artifacts
logs/
sealion_runs/
*_results.json
test_results.txt

# Experimental scripts (root-level temp files)
quick_test_*.py
run_sealion_*.py
test_my_*.py

# Review artifacts (not runtime)
TRACK_C_CORE_ENFORCEMENT_REFERENCE.py
HOUSEKEEPING_PLAN.md

# Patch summaries (move to docs/releases/ instead)
PATCH_*.md

# SEA-LION session logs (runtime exhaust)
cooling_ledger/sealion_forge_sessions.jsonl

# Manual test scripts (ephemeral)
scripts/manual_tests/
cooling_ledger_l4.sqlite3

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/

# Build artifacts
kernel_output.txt

# Logs
logs/
*.log

# Temporary files
tmp_*.py
*_tmp.py
test_*_debug.py
sitecustomize.py
nul
.DS_Store
Thumbs.db
# Secret files (GitHub push protection)
mcp_config_antigravity_constitutional.json
mcp_config_antigravity_constitutional_fixed.json
scripts/setup_antigravity_constitutional_mcp.py

# Temporary/generated directories (v46.1.1 Anti-Pencemaran)
.arifos_clip/
.well_snapshots/
cooling_ledger/
# Local archival parking (do not commit)
archive_local/

# Phase 000-999 forge archives (do not commit)
_ARCHIVE/v63_legacy/

# Local session archives (do not push)
_local_archive/
coverage.xml

# Obsidian plugins (may contain OAuth secrets)
**/.obsidian/plugins/
VAULT999/**/.obsidian/plugins/
vault_999_obsidian/**/.obsidian/plugins/

# v50.5 Runtime Chaos Reduction
arifos/mcp/sessions/*
!arifos/mcp/sessions/.gitkeep
mcp/sessions/*
!mcp/sessions/.gitkeep
# Keep session module code, ignore only archives
aaa_mcp/sessions/archive/*
!aaa_mcp/sessions/archive/.gitkeep
VAULT999/sessions/*
!VAULT999/sessions/.gitkeep
VAULT999/BBB_LEDGER/entries/*
cov_report.txt
coverage_report.txt
Dockerfile.bak
output_log.txt

gha-creds-*.json

# IDE
.vs/

# Archive tarballs (keep compressed versions only)
*.tar.gz

# Additional Python cache
.mypy_cache/
.dmypy.json

# Database files
*.db
*.sqlite
*.sqlite3
# Literal unexpanded env-var placeholder (artefact from misconfigured shell expansion)
${SQLITE_DB_PATH}

# Local environment overrides
.env.local
.env.*.local
.env.production
.env.docker

# Agent ephemeral configs
.kimi_mcp_fixed.json

# Runtime MCP configs (secrets risk — keep at user-level ~/.mcp.json only)
# .agents/mcp.json stays tracked as canonical reference template (uses ${VAR} refs)
.mcp.json
.kimi/mcp.json

# Archived Roadmap Artifacts
ROADMAP/archive/

# Agent workspaces (ephemeral runtime data)
.adal/
.agent/
.agents/
.antigravity/
.augment/
.cache/
.clawhub/
.codebuddy/
.commandcode/
.continue/
.cortex/
.crush/
.factory/
.goose/
.iflow/
.junie/
.kilocode/
.kiro/
.kode/
.mcpjam/
.mux/
.neovate/
.openmcp/
.openhands/
.pi/
.pochi/
.qoder/
.qwen/
arifOS-temp/
.roo/
.trae/
.vibe/
.windsurf/
.zencoder/
.kimi/

# Skills
skills/
skills-lock.json

# Ruff cache
.ruff_cache/

# Backup files
*.backup

# ─── v60.1 Chaos Reduction (2026-02-25) ───

# Remote inspection artifacts (cloned repos, NOT arifOS code)
remote_inspection/

# VPS deployment artifacts (server-side only)
deploy_stack/
arifos-embeddings.service
embed_server.py

# OpenClaw inspection artifacts
openclaw_inspect.json
update_openclaw_config.py

# Node.js (if any node project ever lands here)
node_modules/
package-lock.json
pnpm-lock.yaml
yarn.lock

# Rclone tokens (SECRETS — NEVER commit)
rclone_token.txt
rclone.conf

# Generic secrets catch-all
*.token
*.secret
*credentials*
!**/test*credentials*
# arifOS Governance Reports & Transient Data
e2e_report.txt
pytest-out*.txt
pytest-auth-out*.txt
pytest_*.txt
fastmcp_dir.txt
# FastMCP
.fastmcp/
*.fastmcp.lock
embedding_finetune_data.jsonl
vps_status.txt
ping_test.txt
openclaw_fixed.json

# PowerShell/shell scripts at root (VPS ops tools, ephemeral)
*.ps1
*.sh
!scripts/**/*.ps1
!scripts/**/*.sh
!deployment/**/*.sh

# Gemini / AI session exports at root
*_C.md

# Prevent future archive accumulation (FORGE 2026-03-01)
_ARCHIVE/
*/archive/
*/legacy/

# Embedding models (large files, download at runtime)
aclip_cai/embeddings/*.safetensors
aclip_cai/embeddings/*.bin
aclip_cai/embeddings/*.pt
aclip_cai/embeddings/*.pth

# Tools data output (generated artefacts, not source)
tools/*.csv
*.xlsx
coverage.json
*.xml

# AI session status artefacts (generated, not source)
BROWSER_SETUP_COMPLETE.md
DEPLOYMENT_COMPLETE_*.md
FINAL_SEAL_*.md
Hey.*

# Docker compose backups
docker-compose.yml.backup.*
docker-compose.override.yml

# Local app directories not in scope
333_APPS/
