# arifOS Runtime Configuration — Default
server:
  host: "0.0.0.0"
  port: 8080
  transport: "sse"
  log_level: "INFO"

governance:
  floors_enabled: true
  physics_disabled: false
  vault999_enabled: true

redis:
  url: "redis://localhost:6379"
  session_ttl_s: 3600

search:
  max_results: 5
  timeout_s: 10

embeddings:
  model: "BAAI/bge-m3"
  top_k: 5
