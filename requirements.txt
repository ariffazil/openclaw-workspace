# arifOS Constitutional AI Governance System
# Production dependencies - install with: pip install -r requirements.txt
# For development: pip install -e ".[dev]"

# Core runtime dependencies (required for basic operation)
numpy>=1.20.0
pydantic>=2.0.0
anyio>=4.0.0
starlette>=0.30.0
fastmcp>=2.14.0,<3.0.0
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
sse-starlette>=1.8.2
mcp>=1.0.0
httpx>=0.25.0
prometheus-client>=0.19.0
rich>=13.7.0
asyncpg>=0.29.0               # VAULT999 PostgreSQL backend
sentence-transformers>=2.2.0  # SBERT-based ASI floors (F5, F6, F9)
scikit-learn>=1.3.0           # Cosine similarity for SBERT embeddings

# Web search and reality grounding (for ChatGPT search/fetch tools)
duckduckgo-search>=5.0.0      # DuckDuckGo search integration
playwright>=1.40.0            # Browser automation for web search
beautifulsoup4>=4.12.0        # HTML parsing
requests>=2.31.0              # HTTP client for fetch tool

# System monitoring and observability
psutil>=5.9.0                 # System resource monitoring
python-dotenv>=1.0.0          # Environment variable management
pyyaml>=6.0.0                 # YAML configuration parsing

# Note: For ChatGPT Deep Research integration, ensure playwright browsers are installed:
# playwright install chromium