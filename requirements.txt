# arifOS AAA MCP Server
# Constitutional AI Governance Framework

# Core dependencies
fastmcp>=1.0,<2.0.0  # Pinned: SCAR-002 - v2 API drift, upgrade in v65.1
pydantic>=2.0.0
httpx>=0.24.0

# Database (F1 Amanah)
asyncpg>=0.28.0  # PostgreSQL VAULT999

# ACLIP Tactics Layer (F2 Truth - System observation)
psutil>=5.9.0  # System monitoring (net_monitor, process_guard)
brave-search>=0.1.0  # External ground truth (reality_search)

# Constitutional enforcement
# arifos>=0.1.0  # Local constitutional kernel

# Development
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
ruff>=0.1.0
