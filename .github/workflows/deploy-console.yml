# Build arifOS Metabolic Console (React Dashboard)

name: Build arifOS Console

on:
  push:
    branches: [main, master]
    paths:
      - 'arifosmcp/intelligence/dashboard/**'
      - '.github/workflows/deploy-console.yml'
  workflow_dispatch:

permissions:
  contents: read

# Prevent concurrent builds
concurrency:
  group: "console-pages"
  cancel-in-progress: false

jobs:
  build:
    name: Build Console
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: arifosmcp/intelligence/dashboard/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: arifosmcp/intelligence/dashboard

      - name: Build Console
        run: npm run build
        working-directory: arifosmcp/intelligence/dashboard
