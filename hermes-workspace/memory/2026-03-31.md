# Memory — 2026-03-31

## Context
- Completed AGI_ASI_bot → WAW canon merge; WAW is now the singular canonical workspace and AGI_ASI_bot is archived.
- Forged and sealed the arifOS A–D kernel topology (Layer A gate, Layer B model, Layer C judge, Layer D ledger) as the core governance architecture.
- Discovered OpenClaw cannot currently enforce a true pre-LLM gateway hook; A–D kernel is therefore enforced at protocol/agent level rather than structurally.
- Removed the `agent-zero` container and its docker-compose definition to reduce stack chaos; VPS now runs a leaner, healthier container set.
- Discussed Telegram group behaviour and model routing: desire for "public" model for everyone vs "sovereign" model when Arif calls the bot; concluded this needs routing logic (two agents or subagent plan mode), not just a simple config toggle.

## Insights Gained
- Governance belongs around the model (at IO boundaries), not inside vendor weights; arifOS is constitutional middleware, not a forked model.
- When infrastructure can’t yet enforce (no pre-LLM hook), protocol and agent behaviour must carry the constitutional load; SOUL/AGENT contracts become the enforcement surface.
- Removing unused or unhealthy services (like agent-zero) is thermodynamic discipline, not failure; fewer moving parts = lower entropy and clearer governance.
- Per-sender model routing in group chats ("public" vs "sovereign" brains) is architecturally sound but requires an explicit router layer; current OpenClaw config only supports per-agent defaults, not per-sender engines.

## Decisions
1. Treat the A–D kernel (gate → model → judge → ledger) as the canonical governance topology, even before full gateway-level enforcement exists.
2. Remove the `agent-zero` container and its compose entry from the VPS stack to eliminate noise and an unhealthy dependency on huggingface.co.
3. Keep model-routing desires (GPT‑4o for general group use, MiniMax-like mode for Arif) as design targets to be implemented via routing/subagents rather than forcing it via fragile prompt tricks.

## Preferences Learned
- Arif wants different "modes" in group chats: lighter, general replies for everyone, and a deeper, constitutional mode reserved for his own tagged calls.
- He is comfortable removing components (like agent-zero) once their purpose is understood and confirmed non-essential; reversibility and clarity matter more than keeping every experiment alive.

## Carry Forward
- Any future gateway or Telegram wiring work should aim to realize the A–D kernel structurally (true pre-LLM gate + post-LLM judge + ledger), but until then, agent-level protocol must remain strict.
- When designing new agents or group behaviours, plan for explicit routing (multiple agents or subagents) rather than assuming OpenClaw can switch models per-sender automatically.
- Continue housekeeping on the VPS with the same principle: archive or remove what no longer serves the core arifOS/WAW architecture, while keeping everything reversible where possible.