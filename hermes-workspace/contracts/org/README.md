## 333 Org Topology Contract

This directory holds the canonical **333 ATLAS** topology layer for AAA.

`333-org-units.yaml` defines:

- org units
- parent-child structure
- agent membership
- delegation and governance edges

It turns AAA's registered agents into an institutional graph instead of a flat list.
