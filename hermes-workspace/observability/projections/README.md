# Observability Projections

Projection files summarize event streams into lane- and verdict-aware views.

Primary projection keys:

- `domain_plane`
- `integration_ref`
- `stage`
- `band`
- `verdict`
- `vault_ref`

Use `schemas/audit-projection.schema.json` for normalized projection output.
