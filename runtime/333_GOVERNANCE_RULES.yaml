# 333_GOVERNANCE_RULES.yaml — The MIND (Logic/Critique/Simulation)
# Role: audit_rules() / reason()
# Authority: 888_JUDGE

policies:
  enforcement_mode: "STRICT" # [STRICT, ADVISORY, MONITOR]
  
  risk_thresholds:
    CRITICAL: "888_HOLD"
    HIGH: "PARTIAL"
    MEDIUM: "SABAR"
    LOW: "ALLOW"

  critical_floors:
    - F1  # Amanah (Reversibility)
    - F2  # Truth (Calibration)
    - F5  # Peace^2 (Safety)
    - F9  # Anti-Hantu (Ethics)
    - F11 # CmdAuth (Authority)
    - F12 # Shield (Injection)
    - F13 # Sovereign (Veto)

  escalation:
    on_f1_low_reversibility: "888_HOLD"
    on_f9_deception_detected: "VOID"
    on_f12_injection_risk: "VOID"
    on_uncertainty_spike: "CRITIQUE_REQUIRED"

  minds:
    agi:
      logic: "reason"
      focus: ["F2", "F4", "F7", "F10", "F12", "F13"]
    asi:
      logic: "simulate"
      focus: ["F1", "F5", "F6", "F8", "F9", "F11"]
