## 2026-04-09

- Fixed local Codex config in `/root/.codex/config.toml` by switching from OSS/Ollama (`model = "qwen2.5:0.5b"`, `model_provider = "oss"`, `oss_provider = "ollama"`) to OpenAI (`model = "gpt-5.4"`, `model_provider = "openai"`).
- Confirmed Codex auth source is `/root/.codex/auth.json` with `auth_mode = "apikey"`.
- Confirmed machine-level OpenAI key surfaces also include `/root/arifOS/.env` and `/root/.secrets/vault.env`.
- After updating the OpenAI key, Codex moved past `401 Unauthorized`; current blocker is OpenAI billing/quota (`Quota exceeded. Check your plan and billing details.`), not local config.
- User pasted a raw OpenAI API key into chat; treat that key as exposed in chat history/log surfaces and recommend rotation/revocation after use.
- Added workspace-root boot doctrine file `arifos.init` and wired `AGENTS.md`, `BOOTSTRAP.md`, and `MEMORY.md` so session startup now treats it as mandatory boot law after `SOUL.md` and `USER.md`.
- Recorded Arif's preferred OpenClaw protocol framing: `Salam init to seal vault protocol`.
- Updated `/root/.openclaw/openclaw.json` Telegram config for Arif: switched DM access to `allowlist` with user ID `267378578`, allowlisted group `-1003521544074`, kept group mention gating on, verified Telegram channel health with `openclaw status --deep`, and successfully sent a live test message to the target group.
